<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bts.sahayak">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:logo="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".Pageadapter" />
        <activity android:name=".Homeicon" />
        <activity android:name=".Serviceicon" />
        <activity android:name=".Formicon" />
        <activity android:name=".fourthscreen"></activity>
    </application>

</manifest>